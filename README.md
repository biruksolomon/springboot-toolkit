# spring-auth-core
Build a self-contained, drop-in Spring Boot starter that any future project can pull in as a Maven/Gradle dependency and get a fully working auth system with zero boilerplate. Everything must be overridable — default behavior works out of the box, but the consuming project can replace any piece.
